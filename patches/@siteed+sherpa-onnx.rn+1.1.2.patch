diff --git a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxImpl.kt b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxImpl.kt
index 965ead5..5327c46 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxImpl.kt
+++ b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxImpl.kt
@@ -90,6 +90,17 @@ class SherpaOnnxImpl(private val reactContext: ReactApplicationContext) {
         asrHandler.recognizeFromFile(filePath, promise)
     }
 
+    fun recognizeFromFileWithOptions(filePath: String, options: ReadableMap, promise: Promise) {
+        asrHandler.recognizeFromFileWithOptions(filePath, options, promise)
+    }
+
+    fun cancelAsrFileRecognition(promise: Promise) {
+        // Sets an atomic flag directly on the caller thread; must NOT hop onto
+        // the ASR executor or it would run only after the job it cancels.
+        asrHandler.requestCancelFileRecognition()
+        promise.resolve(true)
+    }
+
     fun releaseAsr(promise: Promise) {
         asrHandler.release(promise)
     }
diff --git a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxModule.kt b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxModule.kt
index c8532cf..2405030 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxModule.kt
+++ b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/SherpaOnnxModule.kt
@@ -25,6 +25,8 @@ class SherpaOnnxModule(reactContext: ReactApplicationContext) :
     @ReactMethod fun initAsr(config: ReadableMap, promise: Promise) = implementation.initAsr(config, promise)
     @ReactMethod fun recognizeFromSamples(sampleRate: Int, audioBuffer: ReadableArray, promise: Promise) = implementation.recognizeFromSamples(sampleRate, audioBuffer, promise)
     @ReactMethod fun recognizeFromFile(filePath: String, promise: Promise) = implementation.recognizeFromFile(filePath, promise)
+    @ReactMethod fun recognizeFromFileWithOptions(filePath: String, options: ReadableMap, promise: Promise) = implementation.recognizeFromFileWithOptions(filePath, options, promise)
+    @ReactMethod fun cancelAsrFileRecognition(promise: Promise) = implementation.cancelAsrFileRecognition(promise)
     @ReactMethod fun releaseAsr(promise: Promise) = implementation.releaseAsr(promise)
     @ReactMethod fun createAsrOnlineStream(promise: Promise) = implementation.createAsrOnlineStream(promise)
     @ReactMethod fun acceptAsrOnlineWaveform(sampleRate: Int, audioBuffer: ReadableArray, promise: Promise) = implementation.acceptAsrOnlineWaveform(sampleRate, audioBuffer, promise)
@@ -74,4 +76,9 @@ class SherpaOnnxModule(reactContext: ReactApplicationContext) :
     @ReactMethod fun testOnnxIntegration(promise: Promise) = implementation.testOnnxIntegration(promise)
     @ReactMethod fun getArchitectureInfo(promise: Promise) = implementation.getArchitectureInfo(promise)
     @ReactMethod fun getSystemInfo(promise: Promise) = implementation.getSystemInfo(promise)
+
+    // No-op stubs so NativeEventEmitter consumers of the module's device
+    // events ('SherpaAsrWindowResult') don't trigger RN warnings.
+    @ReactMethod fun addListener(eventName: String) {}
+    @ReactMethod fun removeListeners(count: Double) {}
 }
diff --git a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/handlers/ASRHandler.kt b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/handlers/ASRHandler.kt
index f5700be..69095d4 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/handlers/ASRHandler.kt
+++ b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/handlers/ASRHandler.kt
@@ -15,6 +15,7 @@ import net.siteed.sherpaonnx.utils.AssetUtils
 import net.siteed.sherpaonnx.utils.AudioExtractor
 import java.io.File
 import java.util.concurrent.Executors
+import java.util.concurrent.atomic.AtomicLong
 
 // Import Sherpa ONNX classes for offline recognition
 import com.k2fsa.sherpa.onnx.FeatureConfig
@@ -59,6 +60,18 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
     private var isRecognizing = false
     private var isStreaming = false
 
+    // Generation-scoped cooperative cancel for file recognition. Each submitted
+    // file job claims a monotonically-increasing generation on the dispatch
+    // thread BEFORE it is queued on the single-thread executor; cancellation
+    // raises a high-water mark. A job is cancelled when cancelUpToGeneration >=
+    // its own generation. This makes cancel per-job: a stale cancel issued
+    // before job B was submitted cannot kill B, and B's own cancel still stops
+    // B even though B runs after a previous job — and there is no global reset
+    // that a successor could erase (the old AtomicBoolean+set(false) reset could
+    // silently lose a cancel for a job draining behind another).
+    private val submittedGeneration = AtomicLong(0)
+    private val cancelUpToGeneration = AtomicLong(0)
+    
     // Keep track of ASR configuration
     private var asrModelConfig: ReadableMap? = null
     
@@ -68,7 +81,6 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
         private const val DEFAULT_FEATURE_DIM = 80
         private const val DEFAULT_DEBUG = false
         private const val DEFAULT_PROVIDER = "cpu"
-        private const val DEFAULT_NUM_THREADS = 2
         private const val DEFAULT_DECODING_METHOD = "greedy_search"
         private const val DEFAULT_MAX_ACTIVE_PATHS = 4
         private const val DEFAULT_CHUNK_SIZE_MS = 50
@@ -96,7 +108,10 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
                 val modelDir = AssetUtils.cleanFilePath(modelConfig.getString("modelDir") ?: "")
                 val modelType = modelConfig.getString("modelType") ?: "transducer"
                 val streaming = if (modelConfig.hasKey("streaming")) modelConfig.getBoolean("streaming") else false
-                val numThreads = if (modelConfig.hasKey("numThreads")) modelConfig.getInt("numThreads") else DEFAULT_NUM_THREADS
+                // Default to half the cores (clamped 2..6): full-core inference
+                // starves the UI thread, a single pair underuses big.LITTLE CPUs.
+                val defaultNumThreads = (Runtime.getRuntime().availableProcessors() / 2).coerceIn(2, 6)
+                val numThreads = if (modelConfig.hasKey("numThreads")) modelConfig.getInt("numThreads") else defaultNumThreads
                 val sampleRate = if (modelConfig.hasKey("sampleRate")) modelConfig.getInt("sampleRate") else DEFAULT_SAMPLE_RATE
                 val featureDim = if (modelConfig.hasKey("featureDim")) modelConfig.getInt("featureDim") else DEFAULT_FEATURE_DIM
                 val decodingMethod = modelConfig.getString("decodingMethod") ?: DEFAULT_DECODING_METHOD
@@ -412,6 +427,8 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
                 whisperConfig.decoder = decoderPath
                 whisperConfig.language = "en"
                 whisperConfig.task = "transcribe"
+                // Per-token timestamps power word-level transcript sync in the player.
+                whisperConfig.enableTokenTimestamps = true
 
                 modelConfig.whisper = whisperConfig
             }
@@ -809,7 +826,8 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
         if (isStreaming) {
             return false
         }
-        return asrModelConfig?.getString("modelType") == "whisper"
+        val modelType = asrModelConfig?.getString("modelType") ?: return false
+        return modelType == "whisper" || modelType == "moonshine" || modelType == "sense_voice"
     }
 
     private fun recognizeSingleOfflineWindow(sampleRate: Int, samples: FloatArray): WritableMap {
@@ -856,6 +874,17 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
             resultMap.putString("text", result?.text ?: "")
             resultMap.putInt("samplesLength", samples.size)
             resultMap.putInt("sampleRate", sampleRate)
+
+            // Include per-token timestamps for precise sync
+            if (result != null && result.timestamps.isNotEmpty()) {
+                val tsArray = Arguments.createArray()
+                for (t in result.timestamps) tsArray.pushDouble(t.toDouble())
+                resultMap.putArray("timestamps", tsArray)
+                val tkArray = Arguments.createArray()
+                for (t in result.tokens) tkArray.pushString(t)
+                resultMap.putArray("tokens", tkArray)
+            }
+
             return resultMap
         } catch (e: Exception) {
             Log.e(TAG, "Error recognizing from samples (offline): ${e.message}")
@@ -870,7 +899,11 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
      * Whisper's offline recognizer only handles windows shorter than 30 seconds.
      * For longer buffers, decode sequential sub-30s windows and join the text.
      */
-    private fun recognizeFromFloatSamplesOffline(sampleRate: Int, samples: FloatArray): WritableMap {
+    private fun recognizeFromFloatSamplesOffline(
+        sampleRate: Int,
+        samples: FloatArray,
+        isCancelled: () -> Boolean = { false },
+    ): WritableMap {
         if (!shouldChunkOfflineWhisper(samples.size, sampleRate)) {
             return recognizeSingleOfflineWindow(sampleRate, samples)
         }
@@ -886,8 +919,16 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
         val transcriptParts = mutableListOf<String>()
         var offset = 0
         var windowIndex = 0
+        var cancelled = false
 
         while (offset < samples.size) {
+            // Cooperative cancel: this in-memory full-decode loop is reachable for
+            // any offline whisper file that took the full-buffer path; honor it so
+            // a cancelled job stops at the next window boundary instead of EOF.
+            if (isCancelled()) {
+                cancelled = true
+                break
+            }
             val end = minOf(offset + samplesPerWindow, samples.size)
             val windowSamples = samples.copyOfRange(offset, end)
             windowIndex += 1
@@ -908,10 +949,11 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
         }
 
         val fullText = transcriptParts.joinToString(" ").trim()
-        Log.i(TAG, "Chunked offline Whisper recognition completed with ${fullText.length} characters")
+        Log.i(TAG, "Chunked offline Whisper recognition completed with ${fullText.length} characters (cancelled=$cancelled)")
 
         val resultMap = Arguments.createMap()
-        resultMap.putBoolean("success", true)
+        resultMap.putBoolean("success", !cancelled)
+        resultMap.putBoolean("cancelled", cancelled)
         resultMap.putString("text", fullText)
         resultMap.putInt("samplesLength", samples.size)
         resultMap.putInt("sampleRate", sampleRate)
@@ -922,60 +964,165 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
     private fun recognizeOfflineWhisperFileInWindows(
         file: File,
         durationUs: Long,
+        startUs: Long,
+        jobId: String,
+        emitWindowEvents: Boolean,
+        isCancelled: () -> Boolean,
     ): WritableMap {
         val windowDurationUs = OFFLINE_WHISPER_MAX_WINDOW_SECONDS * 1_000_000L
-        val totalWindows = ((durationUs + windowDurationUs - 1) / windowDurationUs).toInt()
+        val effectiveStartUs = startUs.coerceIn(0L, durationUs)
+        val remainingUs = durationUs - effectiveStartUs
+        // Clamp into a sane positive range so an over-large / sentinel duration
+        // can't overflow the Int or blow up the JS progress denominator. The
+        // loop stops on reader exhaustion, so this only affects the estimate.
+        val totalWindows = ((remainingUs + windowDurationUs - 1) / windowDurationUs)
+            .coerceIn(1L, 100_000L).toInt()
         Log.i(
             TAG,
             "Chunking offline Whisper file extraction into $totalWindows windows " +
-                "(${OFFLINE_WHISPER_MAX_WINDOW_SECONDS}s each, durationUs=$durationUs)",
+                "(${OFFLINE_WHISPER_MAX_WINDOW_SECONDS}s each, durationUs=$durationUs, startUs=$effectiveStartUs)",
         )
 
+        val reader = AudioExtractor.openWindowReader(file, effectiveStartUs, DEFAULT_SAMPLE_RATE)
+            ?: throw Exception("Failed to open audio window reader for ${file.absolutePath}")
+
         val transcriptParts = mutableListOf<String>()
-        var offsetUs = 0L
+        val segments = Arguments.createArray()
+        var offsetUs = effectiveStartUs
         var totalSamples = 0
-        var sampleRate = 0
+        var sampleRate = DEFAULT_SAMPLE_RATE
         var windowIndex = 0
+        var cancelled = false
 
-        while (offsetUs < durationUs) {
-            val audioData = AudioExtractor.extractAudioWindowFromFile(
-                file = file,
-                startTimeUs = offsetUs,
-                maxDurationUs = windowDurationUs,
-            ) ?: throw Exception("Failed to extract audio window starting at ${offsetUs}us")
+        try {
+            val samplesPerWindow = DEFAULT_SAMPLE_RATE * OFFLINE_WHISPER_MAX_WINDOW_SECONDS
+            while (true) {
+                if (isCancelled()) {
+                    Log.i(TAG, "File recognition cancelled after window $windowIndex/$totalWindows")
+                    cancelled = true
+                    break
+                }
 
-            if (audioData.samples.isEmpty()) {
-                throw Exception("Extracted empty audio window starting at ${offsetUs}us")
-            }
+                val windowSamples = reader.readNextWindow(samplesPerWindow)
+                if (windowSamples == null || windowSamples.isEmpty()) {
+                    // Metadata duration can overestimate the real audio length; an
+                    // empty read at the tail is normal completion, not an error.
+                    if (windowIndex == 0 && effectiveStartUs == 0L) {
+                        throw Exception("Extracted no audio from ${file.absolutePath}")
+                    }
+                    break
+                }
 
-            sampleRate = audioData.sampleRate
-            totalSamples += audioData.samples.size
-            windowIndex += 1
+                sampleRate = reader.outputSampleRate
+                totalSamples += windowSamples.size
+                windowIndex += 1
+                val windowOffsetMs = offsetUs / 1000.0
+                val windowActualDurationUs = windowSamples.size * 1_000_000L / sampleRate
+                val windowEndMs = (offsetUs + windowActualDurationUs) / 1000.0
+
+                Log.i(
+                    TAG,
+                    "Decoding extracted offline Whisper window $windowIndex/$totalWindows " +
+                        "(offsetUs=$offsetUs, samples=${windowSamples.size})",
+                )
 
-            Log.i(
-                TAG,
-                "Decoding extracted offline Whisper window $windowIndex/$totalWindows " +
-                    "(offsetUs=$offsetUs, samples=${audioData.samples.size})",
-            )
+                val windowResult = recognizeSingleOfflineWindow(sampleRate, windowSamples)
+                val windowText = windowResult.getString("text")?.trim().orEmpty()
+                val windowSegs = mutableListOf<Triple<Double, Double, String>>()
+                if (windowText.isNotEmpty()) {
+                    transcriptParts.add(windowText)
+
+                    // Use per-token timestamps if available
+                    val tsArr = windowResult.getArray("timestamps")
+                    val tkArr = windowResult.getArray("tokens")
+                    if (tsArr != null && tkArr != null && tsArr.size() > 0) {
+                        // Group subword tokens into words for per-word player highlighting.
+                        // Whisper's BPE marks a new word with a leading space. SenseVoice
+                        // (SentencePiece) marks it with the '▁' (U+2581) metasymbol;
+                        // sherpa-onnx's SymbolTable already converts a leading '▁' to a
+                        // space for non-byte-BPE tables, but raw '▁' is handled too in
+                        // case the conversion is skipped. Tokens without either marker
+                        // continue the previous word (e.g. "trans" + "form" →
+                        // "transform"). Special tokens like <|...|> are skipped entirely.
+                        // SenseVoice timestamps are CTC emission (start) times, so the
+                        // same next-word-start end estimate below applies to both models.
+                        val wordStarts = mutableListOf<Int>()
+                        for (i in 0 until tkArr.size()) {
+                            val raw = tkArr.getString(i) ?: ""
+                            if (raw.startsWith("<|") || raw.isEmpty()) continue
+                            if (wordStarts.isEmpty() || raw.startsWith(" ") || raw.startsWith("▁")) {
+                                wordStarts.add(i)
+                            }
+                        }
+                        for (wi in wordStarts.indices) {
+                            val s = wordStarts[wi]
+                            val e = if (wi + 1 < wordStarts.size) wordStarts[wi + 1] else tkArr.size()
+                            val sb = StringBuilder()
+                            for (j in s until e) {
+                                val raw = tkArr.getString(j) ?: ""
+                                if (raw.startsWith("<|")) continue
+                                sb.append(raw)
+                            }
+                            val wordText = sb.toString().replace('▁', ' ').trim()
+                            if (wordText.isEmpty()) continue
+                            val startMs = windowOffsetMs + tsArr.getDouble(s) * 1000.0
+                            val endMs = if (e < tsArr.size()) windowOffsetMs + tsArr.getDouble(e) * 1000.0
+                                        else windowOffsetMs + (tsArr.getDouble(tsArr.size() - 1) + 0.3) * 1000.0
+                            windowSegs.add(Triple(startMs, endMs, wordText))
+                        }
+                    } else {
+                        // Fallback: use window boundaries
+                        windowSegs.add(Triple(windowOffsetMs, windowEndMs, windowText))
+                    }
 
-            val windowResult = recognizeSingleOfflineWindow(audioData.sampleRate, audioData.samples)
-            val windowText = windowResult.getString("text")?.trim().orEmpty()
-            if (windowText.isNotEmpty()) {
-                transcriptParts.add(windowText)
-            }
+                    for ((segStart, segEnd, segText) in windowSegs) {
+                        val seg = Arguments.createMap()
+                        seg.putDouble("startMs", segStart)
+                        seg.putDouble("endMs", segEnd)
+                        seg.putString("text", segText)
+                        segments.pushMap(seg)
+                    }
+                }
 
-            offsetUs += windowDurationUs
+                if (emitWindowEvents) {
+                    val evSegments = Arguments.createArray()
+                    for ((segStart, segEnd, segText) in windowSegs) {
+                        val seg = Arguments.createMap()
+                        seg.putDouble("startMs", segStart)
+                        seg.putDouble("endMs", segEnd)
+                        seg.putString("text", segText)
+                        evSegments.pushMap(seg)
+                    }
+                    val ev = Arguments.createMap()
+                    ev.putString("jobId", jobId)
+                    ev.putInt("windowIndex", windowIndex)
+                    ev.putInt("totalWindows", totalWindows)
+                    ev.putDouble("startMs", windowOffsetMs)
+                    ev.putDouble("endMs", windowEndMs)
+                    ev.putString("text", windowText)
+                    ev.putArray("segments", evSegments)
+                    reactContext.emitDeviceEvent("SherpaAsrWindowResult", ev)
+                }
+
+                offsetUs += windowActualDurationUs
+            }
+        } finally {
+            reader.close()
         }
 
         val fullText = transcriptParts.joinToString(" ").trim()
-        Log.i(TAG, "Chunked offline Whisper file recognition completed with ${fullText.length} characters")
+        Log.i(TAG, "Chunked offline file recognition completed with ${fullText.length} characters (cancelled=$cancelled)")
 
         val resultMap = Arguments.createMap()
-        resultMap.putBoolean("success", true)
+        resultMap.putBoolean("success", !cancelled)
+        resultMap.putBoolean("cancelled", cancelled)
         resultMap.putString("text", fullText)
         resultMap.putInt("samplesLength", totalSamples)
         resultMap.putInt("sampleRate", sampleRate)
         resultMap.putInt("chunkCount", totalWindows)
+        resultMap.putDouble("startMs", effectiveStartUs / 1000.0)
+        resultMap.putDouble("processedEndMs", offsetUs / 1000.0)
+        resultMap.putArray("segments", segments)
         return resultMap
     }
     
@@ -1121,41 +1268,102 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
      * Recognize speech from an audio file
      */
     fun recognizeFromFile(filePath: String, promise: Promise) {
-        Log.i(TAG, "Starting recognition from file: $filePath")
+        recognizeFromFileInternal(filePath, 0L, "", false, promise)
+    }
+
+    /**
+     * Recognize speech from an audio file with options:
+     *   startMs           start offset in ms (resume support; segment timestamps
+     *                     stay absolute to the file)
+     *   jobId             opaque tag echoed in every window event so stale
+     *                     events from a superseded job can be dropped in JS
+     *   emitWindowEvents  emit 'SherpaAsrWindowResult' device events per window
+     */
+    fun recognizeFromFileWithOptions(filePath: String, options: ReadableMap, promise: Promise) {
+        val startMs = if (options.hasKey("startMs")) options.getDouble("startMs").toLong() else 0L
+        val jobId = if (options.hasKey("jobId")) options.getString("jobId") ?: "" else ""
+        val emitWindowEvents = if (options.hasKey("emitWindowEvents")) options.getBoolean("emitWindowEvents") else true
+        recognizeFromFileInternal(filePath, startMs.coerceAtLeast(0L) * 1000L, jobId, emitWindowEvents, promise)
+    }
+
+    /**
+     * Request cancellation of an in-flight file recognition. Runs on the
+     * NativeModules dispatch thread ON PURPOSE — hopping onto the executor
+     * would queue the flag-set behind the very job it is meant to stop.
+     * Takes effect at the next window boundary (an in-flight ~29s window
+     * decode cannot be interrupted).
+     */
+    fun requestCancelFileRecognition() {
+        Log.i(TAG, "File recognition cancel requested")
+        // Cancel every job submitted so far (high-water mark). Jobs submitted
+        // AFTER this call have a higher generation and are unaffected.
+        cancelUpToGeneration.set(submittedGeneration.get())
+    }
+
+    private fun recognizeFromFileInternal(
+        filePath: String,
+        startUs: Long,
+        jobId: String,
+        emitWindowEvents: Boolean,
+        promise: Promise,
+    ) {
+        Log.i(TAG, "Starting recognition from file: $filePath (startUs=$startUs, jobId=$jobId)")
+        // Claim this job's generation on the dispatch thread BEFORE it is queued,
+        // so a stale cancel issued before this submission can't kill it and its
+        // own later cancel still stops it. No reset needed inside the executor.
+        val myGen = submittedGeneration.incrementAndGet()
         executor.execute {
+            val isCancelled = { cancelUpToGeneration.get() >= myGen }
             try {
                 // Create File object from path
                 val fileObj = File(AssetUtils.cleanFilePath(filePath))
                 Log.i(TAG, "Using file: ${fileObj.absolutePath}")
 
-                val durationUs = if (isOfflineWhisperModel()) {
-                    AudioExtractor.getAudioDurationUs(fileObj)
-                } else {
-                    null
-                }
-
+                // Probe duration BEFORE decoding so long whisper-like files can take
+                // the windowed-extraction path without first allocating the whole
+                // audio buffer. A 75-min podcast at 16 kHz mono float is ~287 MB —
+                // doing a full-file decode up front OOMs on real Android heaps long
+                // before recognizeOfflineWhisperFileInWindows ever gets to chunk it.
+                val durationUs = AudioExtractor.getAudioDurationUs(fileObj)
                 val windowDurationUs = OFFLINE_WHISPER_MAX_WINDOW_SECONDS * 1_000_000L
-                val resultMap = if (durationUs != null && durationUs > windowDurationUs) {
-                    recognizeOfflineWhisperFileInWindows(fileObj, durationUs)
-                } else {
-                    Log.i(TAG, "Extracting audio from file")
-                    val audioData = AudioExtractor.extractAudioFromFile(fileObj)
+                // Treat a whisper file as "long" when its duration exceeds one
+                // window OR (duration unknown — some VBR/streamed MP3s lack
+                // KEY_DURATION) the file is large. This keeps unknown-duration
+                // long files OFF the full-buffer decode (OOM) AND on the windowed,
+                // cancellable path. The window reader runs to reader exhaustion,
+                // so a missing duration only affects the progress denominator.
+                val isOfflineWhisper = !isStreaming && isOfflineWhisperModel()
+                val isLongWhisperLike = isOfflineWhisper && (
+                    (durationUs != null && durationUs > windowDurationUs)
+                        || (durationUs == null && fileObj.length() > 8L * 1024 * 1024)
+                )
 
-                    if (audioData == null) {
-                        Log.e(TAG, "Failed to extract audio from file")
-                        throw Exception("Failed to extract audio from file")
-                    }
+                val resultMap = if (isCancelled()) {
+                    cancelledResult()
+                } else if (isLongWhisperLike) {
+                    // Use a positive sentinel duration when unknown; the window
+                    // loop stops on reader exhaustion regardless and totalWindows
+                    // is clamped, so this only bounds the progress estimate.
+                    val effDurationUs = durationUs ?: (fileObj.length() / 4 * 1_000_000L / DEFAULT_SAMPLE_RATE)
+                    Log.i(TAG, "Long whisper-like file (${effDurationUs / 1_000_000}s, durKnown=${durationUs != null}) — windowed extraction, skipping full decode")
+                    recognizeOfflineWhisperFileInWindows(fileObj, effDurationUs, startUs, jobId, emitWindowEvents, isCancelled)
+                } else {
+                    Log.i(TAG, "Extracting full audio from file at ${DEFAULT_SAMPLE_RATE}Hz")
+                    val audioData = AudioExtractor.extractAudioFromFile(fileObj, DEFAULT_SAMPLE_RATE)
+                        ?: throw Exception("Failed to extract audio from file")
 
                     val samples = audioData.samples
                     val sampleRate = audioData.sampleRate
                     Log.i(TAG, "Extracted ${samples.size} samples at $sampleRate Hz")
 
-                    if (isStreaming) {
+                    if (isCancelled()) {
+                        cancelledResult()
+                    } else if (isStreaming) {
                         Log.i(TAG, "Recognizing extracted samples with streaming mode")
                         recognizeFromFloatSamplesStreaming(sampleRate, samples)
                     } else {
                         Log.i(TAG, "Recognizing extracted samples with offline mode")
-                        recognizeFromFloatSamplesOffline(sampleRate, samples)
+                        recognizeFromFloatSamplesOffline(sampleRate, samples, isCancelled)
                     }
                 }
 
@@ -1173,6 +1381,13 @@ class ASRHandler(private val reactContext: ReactApplicationContext) {
         }
     }
 
+    private fun cancelledResult(): WritableMap {
+        val resultMap = Arguments.createMap()
+        resultMap.putBoolean("success", false)
+        resultMap.putBoolean("cancelled", true)
+        return resultMap
+    }
+    
     /**
      * Create a persistent online stream for live streaming ASR.
      * Matches upstream OnlineRecognizer.createStream().
diff --git a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/utils/AudioExtractor.kt b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/utils/AudioExtractor.kt
index f3d7907..8302384 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/utils/AudioExtractor.kt
+++ b/node_modules/@siteed/sherpa-onnx.rn/android/src/main/kotlin/net/siteed/sherpaonnx/utils/AudioExtractor.kt
@@ -48,12 +48,15 @@ object AudioExtractor {
             }
             val (audioTrackIndex, audioFormat) = trackInfo
 
-            // Get audio parameters
-            val sourceSampleRate = audioFormat.getInteger(MediaFormat.KEY_SAMPLE_RATE)
-            val channelCount = audioFormat.getInteger(MediaFormat.KEY_CHANNEL_COUNT)
+            // Get audio parameters. Mutable: HE-AAC (SBR/parametric stereo)
+            // reports a core rate/channel count in the container but the decoder
+            // outputs a different rate/channels (announced via
+            // INFO_OUTPUT_FORMAT_CHANGED), so honor the real output format below.
+            var sourceSampleRate = audioFormat.getInteger(MediaFormat.KEY_SAMPLE_RATE)
+            var channelCount = audioFormat.getInteger(MediaFormat.KEY_CHANNEL_COUNT)
             val windowEndUs = maxDurationUs?.let { startTimeUs + it }
             val outputSampleRate = targetSampleRate?.takeIf { it > 0 } ?: sourceSampleRate
-            val resampler = if (outputSampleRate != sourceSampleRate) {
+            var resampler = if (outputSampleRate != sourceSampleRate) {
                 Log.i(
                     TAG,
                     "Resampling extracted audio from ${sourceSampleRate}Hz to ${outputSampleRate}Hz for ${file.name}",
@@ -113,7 +116,22 @@ object AudioExtractor {
                     }
 
                     MediaCodec.INFO_OUTPUT_FORMAT_CHANGED -> {
-                        Log.i(TAG, "Decoder output format changed to ${decoder.outputFormat}")
+                        // Honor the decoder's REAL output format (HE-AAC reports a
+                        // core rate/channels in the container but upsamples via SBR
+                        // / parametric stereo). Delivered before the first output
+                        // buffer, so the resampler can be rebuilt without losing phase.
+                        val of = decoder.outputFormat
+                        val newRate = if (of.containsKey(MediaFormat.KEY_SAMPLE_RATE))
+                            of.getInteger(MediaFormat.KEY_SAMPLE_RATE) else sourceSampleRate
+                        val newCh = if (of.containsKey(MediaFormat.KEY_CHANNEL_COUNT))
+                            of.getInteger(MediaFormat.KEY_CHANNEL_COUNT) else channelCount
+                        if (newCh != channelCount) channelCount = newCh
+                        if (newRate != sourceSampleRate) {
+                            sourceSampleRate = newRate
+                            resampler = if (outputSampleRate != newRate)
+                                StreamingLinearResampler(newRate, outputSampleRate) else null
+                        }
+                        Log.i(TAG, "Decoder output format changed: rate=$newRate ch=$channelCount")
                     }
 
                     MediaCodec.INFO_OUTPUT_BUFFERS_CHANGED -> {
@@ -200,6 +218,243 @@ object AudioExtractor {
         }
     }
 
+    /**
+     * Open a stateful reader that decodes the file sequentially and serves
+     * fixed-size sample windows without re-seeking between windows. Reusing a
+     * single MediaExtractor/MediaCodec avoids the per-window sync-seek (which
+     * lands before the requested time and duplicates audio at window
+     * boundaries) and keeps the resampler phase continuous across windows.
+     */
+    fun openWindowReader(
+        file: File,
+        startTimeUs: Long = 0L,
+        targetSampleRate: Int? = null,
+    ): AudioWindowReader? {
+        var extractor: MediaExtractor? = null
+        var decoder: MediaCodec? = null
+        try {
+            extractor = MediaExtractor()
+            extractor.setDataSource(file.absolutePath)
+
+            val trackInfo = findAudioTrack(extractor)
+            if (trackInfo == null) {
+                Log.e(TAG, "No audio track found in file: ${file.absolutePath}")
+                extractor.release()
+                return null
+            }
+            val (audioTrackIndex, audioFormat) = trackInfo
+
+            val sourceSampleRate = audioFormat.getInteger(MediaFormat.KEY_SAMPLE_RATE)
+            val channelCount = audioFormat.getInteger(MediaFormat.KEY_CHANNEL_COUNT)
+            val outputSampleRate = targetSampleRate?.takeIf { it > 0 } ?: sourceSampleRate
+
+            extractor.selectTrack(audioTrackIndex)
+            var actualSeekUs = 0L
+            if (startTimeUs > 0L) {
+                // PREVIOUS_SYNC lands at or before the requested time; the reader
+                // trims the surplus so the first window starts exactly at
+                // startTimeUs (no re-transcribed overlap on resume).
+                extractor.seekTo(startTimeUs, MediaExtractor.SEEK_TO_PREVIOUS_SYNC)
+                actualSeekUs = extractor.sampleTime.takeIf { it >= 0L } ?: startTimeUs
+            }
+
+            decoder = MediaCodec.createDecoderByType(audioFormat.getString(MediaFormat.KEY_MIME)!!)
+            decoder.configure(audioFormat, null, null, 0)
+            decoder.start()
+
+            val skipOutputSamples = if (actualSeekUs < startTimeUs) {
+                (startTimeUs - actualSeekUs) * outputSampleRate / 1_000_000L
+            } else {
+                0L
+            }
+
+            return AudioWindowReader(
+                extractor = extractor,
+                decoder = decoder,
+                sourceSampleRate = sourceSampleRate,
+                channelCount = channelCount,
+                outputSampleRate = outputSampleRate,
+                skipOutputSamples = skipOutputSamples,
+            )
+        } catch (e: Exception) {
+            Log.e(TAG, "Error opening window reader: ${e.message}")
+            e.printStackTrace()
+            try { decoder?.stop() } catch (_: Exception) {}
+            try { decoder?.release() } catch (_: Exception) {}
+            try { extractor?.release() } catch (_: Exception) {}
+            return null
+        }
+    }
+
+    class AudioWindowReader internal constructor(
+        private val extractor: MediaExtractor,
+        private val decoder: MediaCodec,
+        sourceSampleRate: Int,
+        channelCount: Int,
+        val outputSampleRate: Int,
+        private var skipOutputSamples: Long,
+    ) {
+        // Mutable: HE-AAC (SBR/PS) reports a core rate/channel count in the
+        // container but the decoder outputs a different rate/channels, announced
+        // via INFO_OUTPUT_FORMAT_CHANGED. These must update so the resampler and
+        // mono-mix use the real output format (otherwise audio is time-stretched
+        // / interleave-corrupted and timestamps drift ~2x).
+        private var sourceSampleRate: Int = sourceSampleRate
+        private var channelCount: Int = channelCount
+        private var resampler = if (outputSampleRate != sourceSampleRate) {
+            StreamingLinearResampler(sourceSampleRate, outputSampleRate)
+        } else {
+            null
+        }
+        private val bufferInfo = MediaCodec.BufferInfo()
+        private var carry = FloatArray(0)
+        private var inputDone = false
+        private var outputDone = false
+        private var closed = false
+
+        /**
+         * Decode and return up to [maxSamples] mono samples at [outputSampleRate].
+         * Returns null when the stream is exhausted. Decoder output that spills
+         * past the window boundary is carried over to the next call, so windows
+         * are gapless and overlap-free. EOS is only queued at true end-of-file,
+         * never at window boundaries, so the codec stays reusable.
+         */
+        fun readNextWindow(maxSamples: Int): FloatArray? {
+            if (closed || maxSamples <= 0) return null
+
+            val window = FloatArray(maxSamples)
+            var count = 0
+
+            if (carry.isNotEmpty()) {
+                val take = minOf(carry.size, maxSamples)
+                carry.copyInto(window, 0, 0, take)
+                count = take
+                carry = if (take < carry.size) carry.copyOfRange(take, carry.size) else FloatArray(0)
+            }
+
+            while (count < maxSamples && !outputDone) {
+                if (!inputDone) {
+                    val inputBufferIndex = decoder.dequeueInputBuffer(10_000)
+                    if (inputBufferIndex >= 0) {
+                        val sampleTimeUs = extractor.sampleTime
+                        if (sampleTimeUs < 0L) {
+                            inputDone = true
+                            decoder.queueInputBuffer(inputBufferIndex, 0, 0, 0, MediaCodec.BUFFER_FLAG_END_OF_STREAM)
+                        } else {
+                            val inputBuffer = decoder.getInputBuffer(inputBufferIndex)
+                                ?: throw IllegalStateException("Decoder input buffer is null")
+                            val sampleSize = extractor.readSampleData(inputBuffer, 0)
+                            if (sampleSize < 0) {
+                                inputDone = true
+                                decoder.queueInputBuffer(inputBufferIndex, 0, 0, 0, MediaCodec.BUFFER_FLAG_END_OF_STREAM)
+                            } else {
+                                decoder.queueInputBuffer(inputBufferIndex, 0, sampleSize, sampleTimeUs, 0)
+                                extractor.advance()
+                            }
+                        }
+                    }
+                }
+
+                when (val outputBufferIndex = decoder.dequeueOutputBuffer(bufferInfo, 10_000)) {
+                    MediaCodec.INFO_TRY_AGAIN_LATER -> {
+                        // No output yet.
+                    }
+
+                    MediaCodec.INFO_OUTPUT_FORMAT_CHANGED -> {
+                        // Honor the decoder's REAL output format (HE-AAC reports
+                        // a core rate/channels in the container but upsamples via
+                        // SBR / parametric stereo). MediaCodec delivers this before
+                        // the first output buffer, so the resampler has processed
+                        // zero samples and can be rebuilt without losing phase.
+                        val of = decoder.outputFormat
+                        val newRate = if (of.containsKey(MediaFormat.KEY_SAMPLE_RATE))
+                            of.getInteger(MediaFormat.KEY_SAMPLE_RATE) else sourceSampleRate
+                        val newCh = if (of.containsKey(MediaFormat.KEY_CHANNEL_COUNT))
+                            of.getInteger(MediaFormat.KEY_CHANNEL_COUNT) else channelCount
+                        if (newCh != channelCount) channelCount = newCh
+                        if (newRate != sourceSampleRate) {
+                            sourceSampleRate = newRate
+                            resampler = if (outputSampleRate != newRate)
+                                StreamingLinearResampler(newRate, outputSampleRate) else null
+                        }
+                        Log.i(TAG, "Decoder output format changed: rate=$newRate ch=$channelCount")
+                    }
+
+                    MediaCodec.INFO_OUTPUT_BUFFERS_CHANGED -> {
+                        // Deprecated on newer Android versions, safe to ignore.
+                    }
+
+                    else -> {
+                        if (outputBufferIndex >= 0) {
+                            val outputBuffer = decoder.getOutputBuffer(outputBufferIndex)
+                                ?: throw IllegalStateException("Decoder output buffer is null")
+                            outputBuffer.position(bufferInfo.offset)
+                            outputBuffer.limit(bufferInfo.offset + bufferInfo.size)
+
+                            val sampleData = ByteArray(bufferInfo.size)
+                            outputBuffer.get(sampleData)
+
+                            val frameCount = sampleData.size / (channelCount * 2)
+                            val monoChunk = FloatArray(frameCount)
+
+                            var byteIndex = 0
+                            for (frameIndex in 0 until frameCount) {
+                                var sum = 0f
+                                for (channelIndex in 0 until channelCount) {
+                                    val lo = sampleData[byteIndex].toInt() and 0xFF
+                                    val hi = sampleData[byteIndex + 1].toInt() and 0xFF
+                                    val sample = ((hi shl 8) or lo).toShort()
+                                    sum += sample / 32768f
+                                    byteIndex += 2
+                                }
+                                monoChunk[frameIndex] = if (channelCount > 1) sum / channelCount else sum
+                            }
+
+                            var outputChunk = resampler?.processChunk(monoChunk) ?: monoChunk
+
+                            if (skipOutputSamples > 0L && outputChunk.isNotEmpty()) {
+                                if (outputChunk.size.toLong() <= skipOutputSamples) {
+                                    skipOutputSamples -= outputChunk.size
+                                    outputChunk = FloatArray(0)
+                                } else {
+                                    outputChunk = outputChunk.copyOfRange(skipOutputSamples.toInt(), outputChunk.size)
+                                    skipOutputSamples = 0L
+                                }
+                            }
+
+                            if (outputChunk.isNotEmpty()) {
+                                val take = minOf(outputChunk.size, maxSamples - count)
+                                outputChunk.copyInto(window, count, 0, take)
+                                count += take
+                                if (take < outputChunk.size) {
+                                    val surplus = outputChunk.copyOfRange(take, outputChunk.size)
+                                    carry = if (carry.isEmpty()) surplus else carry + surplus
+                                }
+                            }
+
+                            decoder.releaseOutputBuffer(outputBufferIndex, false)
+
+                            if ((bufferInfo.flags and MediaCodec.BUFFER_FLAG_END_OF_STREAM) != 0) {
+                                outputDone = true
+                            }
+                        }
+                    }
+                }
+            }
+
+            if (count == 0) return null
+            return if (count == maxSamples) window else window.copyOf(count)
+        }
+
+        fun close() {
+            if (closed) return
+            closed = true
+            try { decoder.stop() } catch (_: Exception) {}
+            try { decoder.release() } catch (_: Exception) {}
+            try { extractor.release() } catch (_: Exception) {}
+        }
+    }
+
     fun getAudioDurationUs(file: File): Long? {
         var extractor: MediaExtractor? = null
         return try {
@@ -275,7 +530,7 @@ object AudioExtractor {
         return samples.copyOf(newCapacity)
     }
 
-    private class StreamingLinearResampler(
+    internal class StreamingLinearResampler(
         inputSampleRate: Int,
         outputSampleRate: Int,
     ) {
diff --git a/node_modules/@siteed/sherpa-onnx.rn/src/SherpaOnnxAPI.ts b/node_modules/@siteed/sherpa-onnx.rn/src/SherpaOnnxAPI.ts
index d02bf1c..04e2840 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/src/SherpaOnnxAPI.ts
+++ b/node_modules/@siteed/sherpa-onnx.rn/src/SherpaOnnxAPI.ts
@@ -12,6 +12,7 @@ import type {
   ExtractTarBz2Input,
 } from './types/api';
 import type {
+  AsrFileRecognizeOptions,
   AsrInitResult,
   AsrModelConfig,
   AsrRecognizeResult,
@@ -157,6 +158,17 @@ const nativeAdapter: ApiInterface = {
     return NativeSherpaOnnx.recognizeFromFile(filePath);
   },
 
+  recognizeFromFileWithOptions(
+    filePath: string,
+    options: AsrFileRecognizeOptions
+  ): Promise<AsrRecognizeResult> {
+    return NativeSherpaOnnx.recognizeFromFileWithOptions(filePath, options);
+  },
+
+  cancelAsrFileRecognition(): Promise<boolean> {
+    return NativeSherpaOnnx.cancelAsrFileRecognition();
+  },
+
   releaseAsr(): Promise<{ released: boolean }> {
     return NativeSherpaOnnx.releaseAsr();
   },
diff --git a/node_modules/@siteed/sherpa-onnx.rn/src/services/AsrService.ts b/node_modules/@siteed/sherpa-onnx.rn/src/services/AsrService.ts
index ea8ab5e..09ed343 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/src/services/AsrService.ts
+++ b/node_modules/@siteed/sherpa-onnx.rn/src/services/AsrService.ts
@@ -7,6 +7,7 @@ import type {
   AsrModelConfig,
   AsrInitResult,
   AsrRecognizeResult,
+  AsrFileRecognizeOptions,
 } from '../types/interfaces';
 import { cleanFilePath } from '../utils/fileUtils';
 
@@ -81,6 +82,37 @@ export class AsrService {
     return this.api.recognizeFromFile(cleanFilePath(filePath));
   }
 
+  /**
+   * Recognize speech from an audio file with windowed-progress options
+   * (Android). Per-window results stream via the 'SherpaAsrWindowResult'
+   * DeviceEventEmitter event, tagged with the jobId passed here.
+   */
+  public async recognizeFromFileWithOptions(
+    filePath: string,
+    options: AsrFileRecognizeOptions
+  ): Promise<AsrRecognizeResult> {
+    if (!this.initialized) {
+      throw new Error('ASR is not initialized. Call initialize() first.');
+    }
+    if (!this.api.recognizeFromFileWithOptions) {
+      return this.api.recognizeFromFile(cleanFilePath(filePath));
+    }
+    return this.api.recognizeFromFileWithOptions(cleanFilePath(filePath), options);
+  }
+
+  /**
+   * Request cancellation of an in-flight file recognition. Takes effect at
+   * the next window boundary; the in-flight ~29s window finishes decoding
+   * first. The pending recognizeFromFile* promise resolves with
+   * { success: false, cancelled: true }.
+   */
+  public async cancelFileRecognition(): Promise<boolean> {
+    if (!this.api.cancelAsrFileRecognition) {
+      return false;
+    }
+    return this.api.cancelAsrFileRecognition();
+  }
+
   /**
    * Create a persistent online stream for live streaming ASR.
    * Requires initAsr with streaming=true to have been called first.
diff --git a/node_modules/@siteed/sherpa-onnx.rn/src/types/api.ts b/node_modules/@siteed/sherpa-onnx.rn/src/types/api.ts
index 829aecc..dc72fb2 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/src/types/api.ts
+++ b/node_modules/@siteed/sherpa-onnx.rn/src/types/api.ts
@@ -8,6 +8,7 @@ import type {
   AsrModelConfig,
   AsrInitResult,
   AsrRecognizeResult,
+  AsrFileRecognizeOptions,
   AudioTaggingModelConfig,
   AudioTaggingInitResult,
   AudioTaggingResult,
@@ -160,6 +161,13 @@ export interface ApiInterface {
   initAsr(config: AsrModelConfig): Promise<AsrInitResult>;
   recognizeFromSamples(input: WaveformInput): Promise<AsrRecognizeResult>;
   recognizeFromFile(filePath: string): Promise<AsrRecognizeResult>;
+  /** Android only: windowed file recognition with resume + per-window events */
+  recognizeFromFileWithOptions?(
+    filePath: string,
+    options: AsrFileRecognizeOptions
+  ): Promise<AsrRecognizeResult>;
+  /** Android only: cooperative cancel; takes effect at the next window boundary */
+  cancelAsrFileRecognition?(): Promise<boolean>;
   releaseAsr(): Promise<{ released: boolean }>;
 
   // ASR online streaming primitives
diff --git a/node_modules/@siteed/sherpa-onnx.rn/src/types/interfaces.ts b/node_modules/@siteed/sherpa-onnx.rn/src/types/interfaces.ts
index 1944564..796b5ea 100644
--- a/node_modules/@siteed/sherpa-onnx.rn/src/types/interfaces.ts
+++ b/node_modules/@siteed/sherpa-onnx.rn/src/types/interfaces.ts
@@ -615,6 +615,46 @@ export interface AsrRecognizeResult {
    * Whether an endpoint was detected (streaming mode only)
    */
   isEndpoint?: boolean;
+
+  /**
+   * Timed segments (Android windowed offline recognition). With token
+   * timestamps (whisper, sense_voice) these are per-word; otherwise
+   * (e.g. moonshine) one per ~29s window.
+   */
+  segments?: Array<{ startMs: number; endMs: number; text: string }>;
+
+  /**
+   * True when the job was stopped early via cancelAsrFileRecognition.
+   * Pairs with success: false; segments hold whatever completed.
+   */
+  cancelled?: boolean;
+
+  /**
+   * Number of ~29s windows the file was split into (windowed path only)
+   */
+  chunkCount?: number;
+
+  /**
+   * Absolute start offset in ms the recognition began at (resume support)
+   */
+  startMs?: number;
+
+  /**
+   * Absolute position in ms up to which audio was processed
+   */
+  processedEndMs?: number;
+}
+
+/**
+ * Options for ASR file recognition (Android)
+ */
+export interface AsrFileRecognizeOptions {
+  /** Start offset in ms; segment timestamps stay absolute to the file */
+  startMs?: number;
+  /** Opaque tag echoed in every 'SherpaAsrWindowResult' device event */
+  jobId?: string;
+  /** Emit a 'SherpaAsrWindowResult' device event per decoded window */
+  emitWindowEvents?: boolean;
 }
 
 // ----------------------------------------------------------------------------------
